# ChestCNN hyperparameters (Review 1)
model: cnn
n_classes: 14
dropout_rate: 0.3

training:
  epochs: 10
  batch_size: 32
  learning_rate: 0.0001
  weight_decay: 0.00001
  optimizer: Adam
  scheduler: cosine
  patience: 3
  subset_frac: 0.15
